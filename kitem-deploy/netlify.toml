# Netlify 설정
# 정적 사이트(HTML) + 서버리스 함수 구조

[build]
  # 정적 파일이 있는 폴더 (루트에 index.html)
  publish = "."
  # 함수 폴더
  functions = "netlify/functions"

[functions]
  # Node 런타임
  node_bundler = "esbuild"

# 함수 호출 경로: /.netlify/functions/analyze
# (기본 경로라 별도 리다이렉트 불필요)
